<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.franmelp.golfgpsrefactor">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SDG GPS v2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HoleViz"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AllHoleViewer" />
        <activity android:name=".AAA" />
        <activity android:name=".ShowEarth" />
        <activity android:name=".AdminMenu" />
        <activity android:name=".MenuOrder"></activity>
    </application>

</manifest>
